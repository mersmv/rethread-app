import styled from 'styled-components';

const StyledContainer = styled.div`
	width: 75%;
	margin-left: auto;
	margin-right: auto;
	display: flex;
	justify-content: center;
	flex-direction: row;
`;

const StyledLabel = styled.label`
	display: flex;
	flex-direction: row;
	margin-bottom: 20px;
`;

const StyledLegend = styled.legend`
	display: flex;
	flex-direction: row;
`;
export { StyledContainer, StyledLabel, StyledLegend };
